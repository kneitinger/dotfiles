#!/usr/bin/env python3

import sys
from pyautogui import scroll, hscroll

direction = sys.argv[1]
clicks = int(sys.argv[2])

if direction == 'u':
    scroll(clicks)
if direction == 'd':
    scroll(clicks * -1)
if direction == 'l':
    hscroll(clicks * -1)
if direction == 'r':
    hscroll(clicks)
