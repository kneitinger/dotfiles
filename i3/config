# Kyle Kneitinger's i3 config file
# See http://i3wm.org/docs/userguide.html for extended reference

# WM mod key
set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. After the comma is the fallback font.
# In this case, it handles the iconic glyphs in the workspace names and status
# bar. Uses AUR pkg ttf-font-icons
font pango:Inconsolata for Powerline, Icons 9

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Workspace definitions
set $ws1 "1: term"     # Computer monitor icon
set $ws2 "2: web"      # Globe icon
set $ws3 "3: docs"     # Page with folded corner icon
set $ws4 "4: media"    # Film frame icon

# Application Launchers
bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+m exec vlc
bindsym $mod+n exec firefox
bindsym $mod+p exec pcmanfm
bindsym $mod+u exec uzbl-browser
bindsym $mod+t exec synclient TouchpadOff=$(synclient | grep -c 'T.*pa.*0')

# Autolaunch programs at boot
exec i3-sensible-terminal
exec keepassx
exec firefox

# Configure default workspace for certain applications
assign [class="Firefox"] $ws2
assign [class="[U,u]zbl-core"] $ws2
assign [class="[V,v]lc"] $ws4
assign [class="[E,e]vince"] $ws3

# kill focused window
bindsym $mod+Shift+c kill


# Display dmenu bar w/ patch for height & xft fonts. AUR pkg dmenu-xft-height
bindsym $mod+d exec dmenu_run -h 20 -fn "Inconsolata-10" -nb "$bg" -sb "$fg"
# Display stock dmenu bar
# bindsym $mod+d exec dmenu_run -nb "$bg" -sb "$fg"

# Map volume keys to amixer functions
bindsym XF86AudioRaiseVolume exec amixer -q set Master 3%+
bindsym XF86AudioLowerVolume exec amixer -q set Master 3%-
bindsym XF86AudioMute exec amixer sset Master toggle

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# split in horizontal orientation
bindsym $mod+g split h
# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# Toggle i3 bar
bindsym $mod+b bar mode toggle

# Send to scratchpad
bindsym $mod+Shift+grave move scratchpad
# Show scratchpad
bindsym $mod+grave scratchpad show

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle
# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent
# focus the child container
bindsym $mod+c focus child

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
# Enable double pressing of workspace binding to return to previous space
workspace_auto_back_and_forth yes

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6

# resize window (you can also use the mouse for that)
set $resizemenu "resize: [h] w- [j] h+ [k] h- [l] w+"
bindsym $mod+r mode $resizemenu
mode $resizemenu {
    # These bindings trigger as soon as you enter the resize mode
    bindsym h resize shrink width 10 px or 10 ppt
    bindsym j resize grow height 10 px or 10 ppt
    bindsym k resize shrink height 10 px or 10 ppt
    bindsym l resize grow width 10 px or 10 ppt

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# Book launcher menu
set $bookmenu "books: [p]hysics/[a]nswers [l]ogic [c]alculus"
bindsym $mod+Shift+b mode $bookmenu
mode $bookmenu {
    bindsym p exec evince /home/leaf/Skoo/physics.pdf; mode "default"t stat
    bindsym a exec evince /home/leaf/Skoo/physics_answers.pdf; mode "default"
    bindsym l exec evince /home/leaf/Skoo/logic.pdf; mode "default"
    bindsym c exec evince /home/leaf/Skoo/calculus.pdf; mode "default"
    # Same as above but doesn't exit book mode. Useful for opening many books
    bindsym Shift+p exec evince /home/leaf/Skoo/physics.pdf;
    bindsym Shift+a exec evince /home/leaf/Skoo/physics_answers.pdf;
    bindsym Shift+l exec evince /home/leaf/Skoo/logic.pdf;
    bindsym Shift+c exec evince /home/leaf/Skoo/calculus.pdf;
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# System menu
set $sysmenu "system:  [r]eload re[b]oot [p]oweroff [e]dit conf"
bindsym $mod+q mode $sysmenu
mode $sysmenu {
    # restart i3 inplace (preserves your layout/session)
    bindsym r restart
    bindsym b exec systemctl rebooot
    bindsym p exec systemctl shutdown
    bindsym e exec i3-sensible-terminal -e vim .i3/config; mode "default"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# THEME CONFIGURATION #

# Colorscheme definitions
set $fg #1793b1
set $fgStat #37b3f1
set $bg #333333
set $fgP #0b4866

# Start i3bar to display a workspace bar
bar {
    # Using i3pystatus
    status_command python ~/.i3/status.py
    # Using i3status
    # status_command i3status --config ~/.i3/status.conf
    position top
    mode dock
    # Don't show #s on workspace labels. Looks pleasant & retains functionality
    strip_workspace_numbers yes
    # Holding <Alt> displays the dock if it is hidden with $mod+b
    modifier Mod1
    colors {
        background $bg
        statusline $fgStat
        separator #aaaaaa

        # Property         border  backgr  text
        focused_workspace  $fg     $fg     #ffffff
        inactive_workspace $fg     $fgP    #bbbbbb
    }
}

# Background image
exec feh --bg-scale ~/Pictures/nasaTimelapse.png

# New window's border
new_window pixel 1

# Remove window borders along screen edges
# hide_edge_borders both

# Color scheme
#class                  border  backgr  text    indicator
client.focused          $fgStat $fgStat #ffffff #000000
client.unfocused        $fgP    $fgP    #bbbbbb #000000
client.focused_inactive $fgP    $fgP    #bbbbbb #000000
